# SysGuard AI — direct dependencies only.
# Everything else (numpy, pandas, pyarrow, altair, pydeck, etc.) is a
# transitive dependency of streamlit/plotly and will be resolved
# automatically by pip — pinning them here just invites version
# conflicts with whatever streamlit/plotly actually require.
 
streamlit==1.56.0
psutil==7.0.0
requests==2.32.5
plotly==6.3.0
 
# malware_analysis.py imports these directly (magic, yara) — without
# them installed, file-type disguise detection and YARA rule matching
# silently degrade to no-ops instead of erroring, so don't skip these.
python-magic==0.4.27  # needs libmagic1 at OS level: sudo apt install libmagic1
yara-python==4.5.4
 
